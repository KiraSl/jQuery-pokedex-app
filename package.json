{
  "name": "jquery-pokedex-app",
  "version": "1.0.0",
  "description": "A copy of Pokedex app, but with jQuery",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile:sass": "node-sass sass/main.scss css/styles.css -w"
  },
  "eslintConfig": {
    "env": {
      "es6": true,
      "browser": true,
      "jquery": true
    },
    "extends": [
        "eslint:recommended"
    ],
    "rules": {
        "quotes": ["error", "single"]
    }
  },
  "author": "KS",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^6.0.1",
    "node-sass": "^4.13.1"
  }
}
